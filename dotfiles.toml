[git]

    PackageName = 'git'
    PackageDir = './git'

    [git.links]

        [git.links.config]
            Source = './git/config.toml'
            Target = '$HOME/.gitconfig'

    [git.setup]
        Instructions = [
            'No further setup required'
        ]
